<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.darq37.android_room">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.login.login.LoginActivity" />
        <activity android:name=".activities.register.RegisterActivity" />
        <activity
            android:name=".activities.SplashScreenActivity"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.shared.SharedActivity" />
        <activity android:name=".activities.product.ProductActivity" />
        <activity android:name=".activities.list.ListActivity" />
        <activity android:name=".activities.account.AccountActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>